---
datadog_config:
  tags: "env:{{ env }}, service:{{ service }}, product:{{ product }}, role:{{ role }}"
  log_level: INFO
  apm_enabled: "false" # has to be set as a string

#datadog_config_ex:
#  trace.config:
#    env: dev
#  trace.concentrator:
#    extra_aggregators: version

#datadog_checks:
#  process:
#    init_config:
#    instances:
#      - name: "{{ product }}"
#        search_string: ["{{ product }}"]
